# Normalize line endings to LF in the repo (cross-platform: Windows + WSL).
* text=auto eol=lf

# Lockfiles & generated text stay LF too.
pnpm-lock.yaml text eol=lf

# Binary assets git should never touch.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
