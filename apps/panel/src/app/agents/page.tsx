import { ComingSoon } from "../../components/ComingSoon";

export default function AgentsPage() {
  return <ComingSoon title="Agents" blurb="The capability workers (plan, edit, test, review, document). Phase 1 ships edit; the rest arrive per phase." />;
}
