import { ComingSoon } from "../../components/ComingSoon";

export default function GitPage() {
  return <ComingSoon title="Git" blurb="Worktrees, branches, and PRs across your repos. The Assistant already opens PRs through an isolated worktree per task." />;
}
