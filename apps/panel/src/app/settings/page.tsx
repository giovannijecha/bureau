import { ComingSoon } from "../../components/ComingSoon";

export default function SettingsPage() {
  return <ComingSoon title="Settings" blurb="Repo targets, provider keys, and engine config. For now these are set via the engine's environment variables." />;
}
